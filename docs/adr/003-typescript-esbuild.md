# ADR 003: TypeScript plus esbuild

Status: Accepted

Strict TypeScript covers platform, core, background, bootstrap, and MAIN-world runtime. esbuild emits compact IIFE bundles for browser entrypoints without adding a runtime module loader requirement.
